# NTRP

some weird game ... (for now at least)

# compiling & running

This project uses CMake. Compile and run like this:

    % cd ntrp
    % cd build
    % cmake ..
    % make
    % ./ntrp-srv &
    % ./ntrp-cl
